import * as Phaser from 'phaser';

/** Pixel data console (DASHBOARD view of the pixel design). Built in PV4. */
export class Console extends Phaser.Scene {
  constructor() {
    super('Console');
  }
}
